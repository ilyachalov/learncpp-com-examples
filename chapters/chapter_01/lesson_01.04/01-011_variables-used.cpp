// исходный текст программы сохранен в кодировке UTF-8 без BOM

// variables-used.cpp
// использованные инициализированные переменные

// для получения предупреждения от компилятора используйте следующие ключи:
// – MSVC (cl.exe): /W4
// – g++ (GCC): -Wunused или -Wall

#include <iostream>

int main()
{
    int x{ 5 };

    std::cout << x << '\n'; // переменная теперь использована

    return 0;
}
